import React from 'react';
import { Helmet } from 'react-helmet';

// TODO not useful for now as this application is no SSR based webapp
const Head: React.FC = () => {
  return <Helmet />;
};

export default Head;
